package UI;

import javax.swing.*;
import java.awt.*;

/**
 * Class that initiates the main panel for the Game Page
 */
public class GamePanel extends JPanel {
    /**
     * Main method that sets up size, layouts and background color
     */
    public GamePanel() {
        this.setSize(1920, 1080);

        this.setLayout(null);
        this.setBackground(new Color(173,216,230));;
    }
}
