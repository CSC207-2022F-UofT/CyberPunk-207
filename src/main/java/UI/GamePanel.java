package UI;

import javax.swing.*;
import java.awt.*;

public class GamePanel extends JPanel {
    public GamePanel() {
        this.setSize(1920, 1080);

        this.setLayout(null);
        this.setBackground(new Color(173,216,230));
    }
}
